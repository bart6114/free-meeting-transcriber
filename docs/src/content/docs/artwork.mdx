---
title: "Logo artwork"
description: "A living gallery of Loofah's own logo assets."
---

import AssetGallery from "../../components/AssetGallery.astro";

This page reads directly from Loofah’s canonical icon sources and generated platform icons when the documentation is built. There is no separate gallery inventory to update: changing the app’s logo assets changes this page too.

Native `.icns` and `.ico` containers are excluded because browsers cannot preview them reliably; their browser-friendly source and generated PNG renditions appear below.

<AssetGallery />
