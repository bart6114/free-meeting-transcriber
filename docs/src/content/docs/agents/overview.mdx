---
title: "Agent access"
description: "Give an agent read-only access to local meeting notes through MCP or the CLI."
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";

Free Meeting Transcriber has two local, read-only interfaces for agents. Both read the same vault.

| Interface | Use it when | Output |
| --- | --- | --- |
| MCP | The client can run a local stdio MCP server | Structured tool results and resources |
| CLI | The agent can run shell commands | JSON with `--json`, or readable terminal output |

## A reliable workflow

1. List recent meetings or search with a short phrase.
2. Take the meeting ID from the result. Do not guess it.
3. Read meeting details before fetching a transcript.
4. Fetch the transcript only when the notes and summaries are not enough.

MCP is usually easier because the client receives tool schemas directly. The CLI is a useful fallback when MCP is not available.

## Read-only means read-only

Use these interfaces instead of crawling the vault. They apply the same rules for canonical notes, summaries, speaker names, and transcript rendering.

Neither interface can edit a note, action item, or meeting. The CLI export command is the one exception to in-place read-only behavior: it can create a separate file, and it only replaces an existing file when `--force` is passed.

<CardGrid>
  <LinkCard title="Use the CLI" href="/agents/cli" description="Run commands with JSON output." />
  <LinkCard title="Use MCP" href="/agents/mcp" description="Connect a local stdio server." />
</CardGrid>
